name: "telemetry-test-valid"
version: "1.0"
runtime:
  provider: "openai"
  model_id: "gpt-4o-mini"
agents:
  test_agent:
    prompt: "You are a test assistant."
pattern:
  type: "single"
  config:
    agent_id: "test_agent"
    input: "Test input"
telemetry:
  otel:
    endpoint: "http://localhost:4318"
    service_name: "strands-test"
    sample_ratio: 1.0
